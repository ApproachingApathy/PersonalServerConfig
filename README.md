This file contains the configuration for my personal server.

# Getting Starting
1. Install Google Cloud CLI [link](https://cloud.google.com/sdk/docs/install)
2. Mark `init.sh` as executable.
3. Run `init.sh`.